services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: noise
      POSTGRES_PASSWORD: noise
      POSTGRES_DB: background_noise
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
